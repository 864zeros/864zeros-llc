# THE AETHER MASTER CODEX
**Enterprise:** 864zeros LLC
**Principal Architect:** Jeffrey Martin Conn
**Node Location:** Cresco, Pennsylvania (The "Always-On" PC)
**Current Date:** May 2026

---

## PART 1: The Origin and Philosophy (The "Why")
864zeros LLC is fundamentally rejecting the current industry trend of "prompt wrappers." We are not building fragile scripts that break when the LLM hallucinates. We are building **AETHER** (Adaptive Embodied Thinking : Holistic Evolutionary Runtime).

**The Core Thesis:**
* **Agents are Skills, not Code:** Intelligence should be intrinsic to a localized "Capsule" (a 5-file folder with identity, knowledge, and behavior), not dependent on external logic.
* **Self-Healing:** When an agent is wrong, it must recognize the failure, verify the truth against a deterministic Knowledge Graph, and permanently update its intrinsic knowledge base (`kb.md`).
* **The Immortal Line:** *"The model didn't change. The skill did."* Models are replaceable APIs; the AETHER capsule is the permanent asset.

## PART 2: The Great Migration (The "Where")
Historically, this project was fragmented. Jeff operated out of King of Prussia, managing dozens of isolated tools, Chrome extensions (like Bible-Insights, Chronical, TabVault), and micro-SaaS apps (like GloveBox). 

**The Pivot:**
We moved to Cresco, PA, and unified everything into a massive **Monolith**. We established the **Always-On PC**—a dedicated physical node that runs 24/7.
* **The Problem We Solved Today:** The node was trapped in a Google Workspace MCP auth-loop. We bypassed it, standardized a root `.env` file, and established a direct, stable Telegram heartbeat (`@JC_zbot`) for the node to communicate with Jeff.

## PART 3: The Monolith Architecture (The "How")
Everything exists in `C:\dev\864zeros-llc\`. It is divided into logical "Divisions" mimicking a manufacturing enterprise:
* **`div-1-intelligence/`:** The Brain. Theory, AETHER framework specs, and research.
* **`div-3-factory/`:** The Workshop. Where the raw Python and JS code is built, compiled, and tested.
* **`div-4-gtm/`:** The Storefront. Go-To-Market strategy, playbooks, engagement rules, and metrics.

## PART 4: The Current Flagship: `aether-pulse-x`
Right now, the Factory is entirely focused on **AETHER Pulse X (V1.1)**. It is our social engagement engine, and it is a massive success.

**The Narrative of Pulse X:**
We realized that to sell AETHER, we needed an agent to do the marketing. `pulse_x.py` is an orchestrator that hunts for high-intent leads on X (Twitter).
1.  **Scrape:** Uses Apify to find posts about AI agents, frameworks, and developers.
2.  **Score:** Uses Anthropic's Claude to semantically score posts based on our target audience (Requires score >= 0.65).
3.  **Draft:** Claude drafts a highly technical, 3-sentence reply using our specific "Power Words."
4.  **Notify:** The draft is sent to Jeff via Telegram.
5.  **EXECUTE (Human-in-the-Loop):** **JEFF POSTS MANUALLY.** The system *never* auto-posts.

**The Benchmark (18% CTR):**
We have proven that a highly technical, specific reply generated by this engine achieves an incredible 18% Click-Through Rate. One brilliant reply is worth 1,000 spam bot replies. 

**The Strict Playbook Rules:**
* Always use AETHER Power Words: *Self-healing, Intrinsic, Capsule, Verified.*
* Always link to our Zenodo paper (DOI: 10.5281/zenodo.19212829).
* Maximum of 3 drafts sent per day (Quality control).
* Posts older than 48 hours are heavily penalized in scoring.

## PART 5: The Immediate Engineering Crisis
The `aether-pulse-x` repository was just cloned into `div-3-factory/`. It has been reviewed against V1.1 specs, and there are critical bugs preventing deployment. 

**If you are reading this as an AI Developer, your immediate mandate is to fix these three things in `div-3-factory/aether-pulse-x`:**

* **[Priority 1] The Async "Silent Death" Bug:**
    * *Where:* `pulse_x.py`
    * *What:* The main `run_monitor_mode` loop is synchronous, but it calls `notifier.send_draft_to_telegram()` which is async. It isn't properly `await`ing the network call, meaning the script finishes and kills the background task before the Telegram message actually sends.
    * *Fix:* Implement `asyncio.run()` and properly `await` the notifier methods.
* **[Priority 2] The Recency Scoring Mismatch:**
    * *Where:* `scorer.py` and `tests/test_scorer.py`
    * *What:* There are 5 failing tests. The test suite expects posts > 48 hours old to receive a specific 0.2 score penalty, but the actual logic in `scorer.py` doesn't match the math.
    * *Fix:* Align the logic so the test suite passes.
* **[Priority 3] X-Character Budget Truncation:**
    * *Where:* `drafter.py`
    * *What:* X (Twitter) counts every URL as exactly 23 characters. `drafter.py` is using standard Python `len()`, meaning a 45-character URL is eating up too much of our 280-character budget, causing drafts to be unnecessarily truncated.

## PART 6: System Mandates for Claude Code
1.  **Assume the Persona:** You are the Lead Factory Engineer.
2.  **Respect the Architecture:** Code goes in `div-3`, strategy goes in `div-4`. Do not mix them.
3.  **KISS Principle:** Keep it simple, stupid. No complex vector databases, no massive framework overhauls. We use standard libraries and straight APIs.
4.  **No Hallucinated Data:** Only use the real Telegram token, real Apify logic, and real AETHER concepts.

**End of Codex.**