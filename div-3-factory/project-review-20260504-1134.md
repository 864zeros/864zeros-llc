# Project Review: 864zeros Workspace
**Date:** 2026-05-04
**Reviewer:** Gemini CLI Agent

---

## 1. Executive Summary
The workspace located at `C:\Users\I820965\dev\864zeros` is a highly opinionated, systemized application engine and methodology for building Chrome Extensions and potentially future web/mobile apps. Operated under the brand "864zeros LLC" (and sub-brand "OIA" for ADHD-friendly UX), this project uses AI CLI agents (Claude Code, Gemini CLI) to autonomously generate software using a strict, document-driven build kit (`864z-build-kit`).

The project is intensely focused on privacy, zero-knowledge architectures, local-first data storage, and zero telemetry. It embraces a "Vulture Strike Philosophy" targeting stagnant SaaS products by offering 20% of the features that solve 80% of user pain, specifically acting as "Rescue", "Sunset", or "Liberation" tools.

## 2. Core Architecture & Build Kit (`864z-build-kit`)

### 2.1 The "Lego Brick" Architecture
The build system relies on a unified core and separates universal rules from platform-specific rules.
- **Universal (`CLAUDE-base.md` / `GEMINI-base.md`):** Mandates KISS principles, ADHD-friendly UX (single actions, no guilt mechanics, progressive disclosure), and the OIA Design System.
- **Platform-Specific (`CLAUDE-extension.md`):** Enforces Manifest V3 Chrome Extension patterns. All extensions in this workspace **MUST BE SIDE PANEL EXTENSIONS** (`chrome.sidePanel`).

### 2.2 Security & The "Vulture Strike"
The `CLAUDE-extension.md` defines aggressive security constraints:
- **Zero-Network Default:** Service workers must not make external calls unless explicitly approved.
- **BRK-CRYPTO-001:** Mandates AES-256-GCM + PBKDF2 encryption, with the master password never stored.
- **Migration Path:** Every extension must parse competitor exports on day one and show an "Aha Moment" (security score, savings audit).
- **Service Worker Limits:** Prohibits the use of `"type": "module"` in the manifest background section (although noted that some existing extensions like `clipboard` and `webinsights` currently still have `"type": "module"` defined in their manifests, highlighting a slight deviation from the newly hardened rules).

### 2.3 Phased Execution
The kit forces AI agents to build in strict phases:
1. Scaffold -> 2. Security -> 3. Migration -> 4. UI Shell -> 5. Features -> 6. Polish -> 7. Proof.
Nothing ships until Phase 7 (Proof) passes.

## 3. Project Integrity & LLM Guardrails
A unique and critical component of this workspace is `CLAUDE-INTEGRITY.md`. It serves as a stark warning and strict rulebook for AI assistants, born from a past failure where an LLM ignored established side-panel patterns and hallucinated DevTools extensions instead. 

**Mandates from `CLAUDE-INTEGRITY.md`:**
1. Do not lie about reading references.
2. The pattern is **SIDE PANEL**. Period. Look at `clipboard/` and `webinsights/`.
3. If specs conflict with patterns, ask the user. Do not guess.

## 4. Current State & Extensions Directory
The workspace contains several active/completed extensions in the `extensions\` directory, many aligned with the OIA brand (focus/ADHD tools):

- **clipboard:** A reference side-panel extension.
- **WebInsight(s):** An AI-powered web content capture tool used heavily as a reference architecture.
- **OIA Focus Suite:** `oia-focus-note`, `oia-focus-timer`, `oia-focus-wall`, `oia.focus.signal`, `oia.focus.sound`.
- **Other utilities:** `Bible-Insight`, `Signal2Noise`, `TabVault`, `Time2Focus`, `TuneOut2FocusIn`, `who-is-watching`.

## 5. Observations & Recommendations
1. **Manifest Discrepancy:** The hardened `CLAUDE-extension.md` (v2.0, updated March 2026) strictly forbids `"type": "module"` in the service worker manifest config to avoid ES import registration errors. However, a review of `clipboard/manifest.json` and `webinsights/manifest.json` shows both still use `"type": "module"`. *Recommendation: Audit existing extensions to align with the new hardened v2.0 standards.*
2. **AI Provider Abstraction:** The system mandates a unified `api-client.js` that strips PPI (Personal Private Information) and routes to Gemini or Claude. This ensures users are never locked into a single LLM provider and guarantees local redaction.
3. **Monetization:** Uses a standard Tier Model (Free, Starter, Pro, Power) powered by Stripe (`ExtPay.js` seen in `clipboard`), with a firm promise of "No Ads. Ever."

---
*Report generated by Gemini CLI in C:\Users\I820965\dev\864zeros*
